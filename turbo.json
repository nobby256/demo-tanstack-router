{
  "$schema": "./node_modules/turbo/schema.json",
  "tasks": {
    "generate": {
      "dependsOn": ["^build"],
      "outputs": [".generated/**"]
    },
    "check": {
      "dependsOn": ["generate", "^check"],
      "outputs": []
    },
    "build": {
      "dependsOn": ["generate"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["generate"],
      "cache": false,
      "persistent": true
    },
    "format": {
      "dependsOn": ["generate"],
      "cache": false
    },
    "test": {
      "dependsOn": ["build", "^test"],
      "outputs": []
    },
    "clean": {
      "dependsOn": ["^clean"],
      "cache": false
    }
  }
}
